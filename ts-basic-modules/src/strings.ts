export function formatYen(price: number): string {
    return String(price) + " yen";
}

export function greet(name: string): string {
    return "Hello, " + name + "!";
}
export function toUpper(a: string) { return a.toUpperCase() }